# Rust toolchain configuration
# Ensures consistent Rust version across all contributors

[toolchain]
channel = "stable"
components = ["rustfmt", "clippy"]
targets = [
    "aarch64-linux-android",
    "armv7-linux-androideabi", 
    "x86_64-linux-android",
]
